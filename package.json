{
  "scripts": {
    "sync-skills": "./scripts/sync-ai-specs-skills.sh",
    "verify:frontend": "pnpm --dir frontend run typecheck && CI=true pnpm --dir frontend test"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "prisma": {
    "schema": "backend/prisma/schema.prisma"
  },
  "devDependencies": {
  }
}
